# 複製成 monitoring/.env 後填值。docker compose 會自動讀同目錄的 .env。

# Grafana 管理者帳密（首次登入 http://localhost:3002 用）。
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=change-me

# ⚠️ 必須與 Augur bridge 的 .env 之 WEBHOOK_SECRET「完全一致」。
# Grafana 告警會用這串當 Authorization: Bearer，bridge 才認得。
WEBHOOK_SECRET=change-me-to-match-bridge
